[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "imagegen"
version = "0.1.0"
description = "AI 生图 CLI：自定义 API 配置，指定输出路径与提示词"
requires-python = ">=3.8"

[project.scripts]
imagegen = "generate:main"

[tool.setuptools]
py-modules = ["generate"]
